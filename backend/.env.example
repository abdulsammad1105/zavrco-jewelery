# PostgreSQL connection string (Local Docker / Postgres fallback)
DATABASE_URL=
# Signs login session cookies
SESSION_SECRET=

# The single fixed admin login
ADMIN_EMAIL=
ADMIN_PASSWORD=

# The frontend's origin, for CORS
FRONTEND_URL=http://localhost:3000

# Port this server listens on
PORT=4000

NODE_ENV=development
