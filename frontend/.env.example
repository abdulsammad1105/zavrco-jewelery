# URL of the backend API (required — the frontend has no database access of its own)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Used only for sitemap.xml / robots.txt (optional, defaults to https://zavr.co)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
